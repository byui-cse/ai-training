# Application Configuration
PROJECT_NAME=Auction Website
API_V1_STR=/api/v1
SECRET_KEY=your-secret-key-change-this-in-production
ACCESS_TOKEN_EXPIRE_MINUTES=30
REFRESH_TOKEN_EXPIRE_DAYS=7

# Server Configuration
SERVER_NAME=Auction Website API
SERVER_HOST=http://localhost

# CORS Origins (comma-separated)
BACKEND_CORS_ORIGINS=http://localhost:3000,http://localhost:8000

# Database Configuration
POSTGRES_SERVER=localhost
POSTGRES_USER=auction_user
POSTGRES_PASSWORD=auction_password
POSTGRES_DB=auction_db
POSTGRES_PORT=5432

# Alternative: Use full DATABASE_URL
# DATABASE_URL=postgresql://auction_user:auction_password@localhost:5432/auction_db

# Email Configuration (for future notifications)
SMTP_TLS=True
SMTP_PORT=587
SMTP_HOST=smtp.gmail.com
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=your-email-password
EMAILS_FROM_EMAIL=your-email@gmail.com
EMAILS_FROM_NAME=Auction Website

# Redis Configuration (for future caching)
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=

# Development/Testing
TESTING=false
